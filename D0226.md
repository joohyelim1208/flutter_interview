# 플러터 면접 스터디

## 숙련
- 0226 : Q. 플러터 앱개발에서 '상태'가 무엇을 의미하는지. (이전 질문인 스테이트위젯과 관련해서 이어서 답변에 따라 꼬리질문도 달라진다.)
핵심: 선언적(Declarative) UI 프로그래밍의 핵심을 이해하고 있는지 확인하는 중요한 질문
추가하기: 정의, 특징, 관리방법으로 나누어 답변

A. 플러터에서 상태에 대해 정의하자면 'UI를 렌더링하는데 필요한 데이터'를 의미합니다.
'위젯이 빌드되는 시점에 읽을 수 있고, 위젯의 수명이 끝나기 전 까지 변경될 수 있는 모든 데이터'로 사용자와의 상호작용에 따라 변경되는 값을 말합니다.

예를들면 앱 화면에서 프로필 수정, 체크박스 선택, 다크모드 전환과 같이 변화하는 모든 데이터가 '상태'이며 이 상태가 변할 때 위젯을 다시 빌드해서 화면을 업데이트 합니다.
상태에는 2가지 종류가 있는데
단일 위젯 내부에서만 쓰이는 '로컬상태', 앱의 여러 부분에서 공유되는 '전역상태'입니다.
로컬상태의 경우에는 보통 스테이트풀위젯의 내부에서 setState()로 관리합니다.
전역상태의 경우에는 프로바이더, 리버팟, 블록 같은 상태관리 라이브러리가 필요합니다.

상태관리는 이 상태들을 효율적으로 저장하고, 변화할 때 화면을 적절히 대응하게 하는 기술로서
상태관리가 중요한 이유는 값이 바뀔 경우 기존 위젯을 버리고 새 상태를 반영한 새 위젯을 가져와야 하는데 이 과정을 효율적으로 일어나도록 설계가 필요합니다.

만약 setState()로만 앱을 만들 경우에 앱의 규모가 커지는 순간 Prop Drilling 현상이 벌어지게 됩니다.
프롭드릴링은 상위 위젯에 있는 생태 데이터를 가장 깊이 있는 하위 위젯으로 전달하기 위해 중간에 있는 위젯들을 거쳐가며 데이터를 계속 넘겨주게 됩니다.

이렇게 단방향으로 데이터의 흐름이 이어지게 되면 중간 위젯들의 코드에 불필요한 인자들이 생겨 복잡해지고
그로인해 코드의 가독성도 저하되어서 데이터의 시작과 끝 지점을 파악하기도 어려워지며
중간위젯들이 하위위젯의 데이터 구조에 의존하게 되어서 수정도 어려워지게 됩니다.

데이터를 필요하지 않는 중간위젯들을 거쳐 데이터를 하위로 전달하는 구조적인 비효율성은 앱이 커질수록 유지보수에 더 어려움을 발생시킵니다.

이런 문제들을 해결하기 위해 리버팟이나 블록 같은 도구를 사용해 데이터의 흐름을 제어하고 UI코드와 비즈니스 로직을 분리하면 가독성도 높이고 데이터를 공유하기도 편리하고 불필요한 리빌드도 방지합니다.

예를들면 프로바이더같은 라이브러리를 쓰면 위젯이 직접 데이터를 가져옵니다.
```dart
// 1. 데이터 저장소(ChangeNotifier) 정의
class TodoProvider extends ChangeNotifier {
  List<TodoEntity> _todos = [];
  List<TodoEntity> get todos => _todos;

  // 할 일을 추가하는 비즈니스 로직
  void addTodo(TodoEntity todo) {
    _todos.add(todo);
    notifyListeners(); // 2. "나 데이터 바뀌었어! 이거 쓰는 위젯들 다 다시 그려!"라고 방송함
  }
}

// 3. UI에서 사용
class TodoListWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // 저장소를 구독(Watch)함. 데이터가 바뀌면 이 부분만 자동으로 리빌드됨.
    final todoProvider = context.watch<TodoProvider>();

    return ListView.builder(
      itemCount: todoProvider.todos.length,
      itemBuilder: (context, index) => Text(todoProvider.todos[index].title),
    );
  }
}
```

정리해서 상태는 앱의 현재 UI를 결정하는 소스코드이며, 플러터 개발은 이 상태를 언제, 어디서, 어떻게 효율적으로 변화시키고 전파할 것인가 설계하는 과정이라고 할 수 있습니다.

***
추가질문.
1. 해결 방안 (상태 관리)
플러터에서는 프롭 드릴링을 해결하기 위해 여러 상태 관리 기법을 사용
1) Provider / Riverpod: 데이터가 필요한 하위 위젯이 직접 상위의 데이터를 가져올 수 있게함
2) InheritedWidget: 플러터의 내장 메커니즘으로, 위젯 트리 하위로 데이터를 효율적으로 전달
3) Bloc / Cubit: 비즈니스 로직과 UI를 분리하여 중앙에서 상태를 관리

조금 더 핵심만 간략하게 설명하면 좋다.