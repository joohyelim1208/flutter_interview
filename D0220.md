# 플러터 면접 스터디

## 운영체제
- 0220 : Q. 동기와 비동기의 차이에 대해 설명하세요.
핵심: 비동기가 왜 필요한지 / 이게 UI화면에 어떤 영향을 주는지
추가하기:

A. 플러터 다트에서 동기는 코드가 순차적으로 실행되며, 앞선 작업이 끝날 때 까지 다음 작업이 멈춰있는 방식을 의미하며, 비동기는 시간이 오래걸리는 작업인 네트워크 통신이나 DB 조회 등 을 기다리는 동안 다른 작업을 먼저 처리할 수 있도록 흐름을 비워주는 방식을 의미합니다.

비동기처리가 중요한 이유는 플러터는 '싱글스레드'(프로세스 실행 단위가 하나뿐인 것. 한번에 하나의 코드라인 실행) 기반인 '이벤트 루프'(쉬지않고 할 일 목록을 확인하는 반복문)에서 동작하는데 만약 4~5초가 걸리는 데이터 다운로드를 동기방식으로 처리하면 5초동인 이벤트루프가 멈추게 되어 화면이 멈추는 UI블로킹 현상이 발생합니다.
사용자의 입장에서는 이 부분이 에러처럼 느껴지게 되는데 비동기를 사용하면 데이터를 가져오는 동안에도 화면의 애니메이션이나 스크롤이 매끄럽게 유지됩니다.

비동기를 구현하는 방식은 미래에 완료될 값인 `Future`, 해당함수가 비동기적으로 동작함을 선언하는 `async`, 비동기 작업이 완료될 때 까지 기다리되, 스레드를 점유하지 않고 양보하는`await`, 단일값이 아닌 데이터의 흐름을 다룰 때는 `stream`을 사용합니다.

퓨쳐와 스트림의 차이는
퓨쳐는 단발성 응답으로 정보를 한번 불러오는 작업에 적합하며, 스트림은 지속적이고 연속적인 데이터의 흐름입니다. 예를 들면 실시간 채팅 알림이나 유튜브 영상 스트리밍처럼 여러번 데이터가 들어올 때 사용합니다.

제가 지금 연습하고 있는 다트(싱글 스레드 언어) 영화예매 콘솔 프로그램 예제에서도 `stdin.readLineSync()`를 사용하고 있는데 이것또한 동기함수로 사용자의 입력을 기다리는 동안 프로그램 전체가 멈춰있는 상태가 되게 됩니다.
실제 앱에서는 네트워크에서 영화 데이터를 받아오는 동안 화면이 멈추면 안되기 때문에 반드시 비동기를 써야 합니다.


***
추가질문.
1. CPU를 많이 쓰는 계산 작업도 비동기로 해결가능?
아니다. 그럴 땐 데이터를 대용량으로 처리하는 `isolate`라는 별도의 독립적 공간을 생성해서 처리해야 됩니다.
스레드와 다른 점은 비동기는 기다리는 일에는 효과적이지만 CPU를 많이 사용하는 복잡한 계산은 메인 스레드를 점유하여 UI를 멈추게 할 수 있기 때문에 아이솔레이트를 생성해서 해결합니다.
스레드와의 차이점은 메모리 공유여부 입니다.
일반적인 스레드는 여러 스레드가 같은 메모리 공간을 공유해서 효율적이지만 동시에 같은 데이터를 수정할 때 데이터가 꼬일 수 있어 잠금처리가 복잡합니다.
이아솔레이트는 독립적인 메모리와 이벤트루프를 가져 서로 데이터를 공유하지 않기 때문에 메모리 충돌 걱정이 없으며, 오직 메시지(port)를 주고받는 방식으로만 소통합니다,

2. `isolate`를 사용하는 가장 적절한 때는?
UI렌더링에 영향을 줄 정도로 무거운 작업이 있을 때 사용한다.
수만개의 데이터를 파싱하는 JSON처리, 대용량 이미지나 비디오의 인코딩/디코딩, 복잡한 암호화 알고리즘을 실행할 때 사용합니다.

3. `isolate`간 데이터를 주고 받는 방식은?
`sendPort`와 `ReceivePort`를 사용합니다.
비동기 과정으로 이루어지며 데이터를 보내는 쪽은 포트에 메시지를 던지고, 받는 쪽은 스트림 형태로 메시지를 감지하여 처리합니다.

실제 프로젝트에서는 `isolate`를 구현하는 것도 방법이지만, 일회성 작업이라면 플러터에서 제공하는 `compute()`함수를 사용해서 더 간편하게 멀티 스레딩 효과를 낼 수 있습니다. 상황에 따라 적절한 비동기 방식과 아이솔레이트 활용 방안을 고민해 끊김없는 사용자 경험을 제공하는 것을 중요하게 생각합니다.

4. `readLineSync()`와 `readLine()` 의 차이를 알면 동기, 비동기 방식을 차이점을 명확히 알 수 있다.
두 방식의 차이를 **'작업의 흐름이 멈추는가(Blocking)'**와 **'결과를 기다리는 방식'**에 초점을 맞추어 보면

1) 동기 방식: `stdin.readLineSync()`
동기 방식은 **"설계된 순서대로 하나씩 실행"**하는 방식입니다. 함수가 값을 반환할 때까지 프로그램의 모든 실행이 그 자리에서 멈춤(Blocking) 상태가 됩니다.
```dart
void main() {
  print('1. 이름 입력 대기중...');
  
  // 프로그램이 여기서 멈춤. 사용자가 엔터를 칠 때까지 2번으로 넘어가지 않는다.
  String? name = stdin.readLineSync(); 
  
  print('2. 입력 완료: $name');
  print('3. 다음 작업 진행');
}
```

2) 비동기 방식: `stdin.transform(...).listen()`
비동기 방식은 "작업을 예약해두고 다음으로 넘어가는" 방식입니다. 데이터가 들어올 때까지 기다리지 않고 다른 코드를 먼저 실행하며, 데이터가 준비되면 그때 별도로 처리한다.

사실 stdin에는 readLine()이라는 단일 함수는 없지만, Stream을 이용해 비동기적으로 입력을 읽는 방식이 그 역할을 대신함.
```dart
void main() {
  print('1. 비동기 입력 대기 시작');

  // 입력을 기다리는 동안 프로그램은 멈추지 않고 아래로 흐릅니다.
  stdin.transform(utf8.decoder).transform(const LineSplitter()).listen((line) {
    print('입력 결과 도착: $line'); // 나중에 입력이 들어오면 실행됨
  });

  print('2. 입력 대기 중에도 나는 내 할 일을 한다!');
  print('3. 프로그램 흐름 계속 진행 중...');
}
```

| 구분 | readLineSync() (동기)	| 비동기 스트림 처리 (비동기) |
| :---: | :---: | :---: |
| 반환 타입 |	String? (직접 값 반환) | Stream<String> (데이터의 흐름) |
| 제어권 | 작업 완료 시까지 차단(Blocking) | 즉시 반환 비차단(Non-blocking) |
| 주된 용도 | 간단한 CLI 도구, 순차적 입력 필요 시	| 서버 통신, 실시간 UI 업데이트, 멀티태스킹 |
