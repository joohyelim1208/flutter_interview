# 플러터 면접 스터디

## OOP
- 0224 : Q. 추상화
핵심: 복잡한 내부 로직을 숨기고 사용자에게 꼭 필요한 기능만을 노출하는 프로그래밍 원칙
추가하기:

A. 객체지향 프로그래밍 핵심 원칙 중 하나로 객체의 복잡한 내부 구현 로직을 숨기고 사용자에게 필요한 기능만을 노출하는 것을 의미합니다.
어떻게 작동하는지 보다 무엇을 하는지에 집중하는 것으로 사용자는 어떻게 앱이 동작하는지 기술은 모르더라도 기능적인 부분만 알면 되는 것 처럼 이런 것을 추상화된 인터페이스라고 합니다.

변하는 것과 변하지 않는 것을 분리해 소프트웨어의 확장성을 확보하는 가장 기본적인 도구입니다.

추상화가 필요한 이유는 3가지인데 공통기능을 묶어 여러곳에서 사용가능한 코드의 재사용성을 높이고, 내부 로직이 변경되어도 외부에서 호출하는 인터페이스는 변하지 않으므로 코드의 수정을 최소화 할 수 있습니다.
구체적인 구현체에 의존하는 것이 아니라 추상화 된 인터페이스에 의존하게 함오로써 클래스 간 의존성을 줄입니다.
실제 DB나 API서버 대신 가짜(Mock) 객체를 주입해 유닛테스트가 용이합니다.

실무에서는 레포지토리 페턴을 구현할 때 추상화를 적극적으로 활용하는데 예를들면 로그인 기능 같은 것을 구현할 때,
공통으로 가질 추상클래스를 만든 다음 메서드를 정의합니다.
그리고 실제로 동작하는 클래스를 만들어서 구체적인 로그인 방식을 만듭니다. 
UI(블록이나 프로바이더 등)에서는 어떤 DB를 쓰는지 알 필요 없이, 레포지토리라는 인터페이스를 통해 데이터를 요청하기만 하면 됩니다.

나중에 서버를 파이어베이스에서 다른 것으로 교체하더라도 UI코드는 수정할 필요 없게 됩니다.

로그인 기능에서 어디로 로그인할지 카카오, 구글, 이메일에 따라 로직이 달라지는데 이때 추상화를 사용하면 UI코드를 건드리지 않고도 로그인 수단을 늘릴 수 있습니다.


***
추가질문.
1. 추상클래스 Absctrct와 인터페이스Interface의 차이는 무엇인가요?
다트는 별도의 interface키워드가 없지만 대신 모든 클래스가 암시적 인터페이스 역할을 할 수 있습니다.
추상클래스는 extends상속을 쓰면 부모의 기능을 자식이 물려받으며 확장하는 개념이고, 
(예시. A는 B의 일종이다.)
인터페이스는 implements구현을 쓰면 부모의 기능을 물려받는 것이 아니라 모든 메서드를 처음부터 끝까지 직접 구현하며 모든 규칙을 다 지키라고 강제하는 설계도 입니다.
(예시. A는 B의 기능을 할 수 있다.)