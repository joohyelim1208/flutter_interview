# 플러터 면접 스터디

## 운영체제
- 0225 : Q. 플러터에서 Isolate란 무엇인가요?
핵심: 다트의 싱글스레드 운영방식과 비동기처리를 얼마나 이해하고 있는지
추가하기:

A. 아이솔레이트는 독립적인 작업 단위로 일반적인 스레드와 유사하지만 메모리를 공유하지 않는다는 점이 차이점입니다.
아이솔레이트는 각각 자체 메모리 루프와 이벤트 루프를 가지고 있어 서로 상태에 직접 접근이 불가합니다.
대신 메시지 패싱이라는 방식을 통해 소통합니다.

아이솔레이트를 사용하는 이유는 UI스레드가 무거운 대용량 JSON파싱이나 이미지 가공 등으로 버벅임이 발생하는 것을 방지하기 위해 별도의 아이솔레이트를 생성하여 백그라운드에서 병렬처리를 수행합니다.
다트에서 보통 Future나 async/await같은 비동기 처리를 하더라도 UI 멈춤을 방지하려고 하지만 싱글스레드에서는 여전히 돌아갑니다.
그래서 복잡한 CPU를 사용하는 작업에서는 아이솔레이트가 필수입니다.



예시를 들면 
간단한 작업으로는 compuse()함수가 있는데 외부함수에 데이터를 던지고 결과만 받아오고 싶을 때 사용합니다.
함수가 종료되면 아이솔레이트도 즉시 소멸되므로 일회성 작업에 최적입니다.

Isolate.spawn() port방식은 메인과 새 아이솔레이트과 서로 편지를 주고받는 방식으로 양방향 통신이 필요할 때 사용합니다.

Top-level Function: 아이솔레이트 내에서 실행될 함수는 반드시 전역 함수이거나 static 함수여야 합니다. (인스턴스 메서드는 메모리 공유 문제로 전달이 불가하기 때문입니다.)

Port 통신: 아이솔레이트 간에는 변수를 직접 수정할 수 없으므로, SendPort로 데이터를 보내고 ReceivePort로 받아야 한다는 점을 강조하세요.

성능: 무분별한 아이솔레이트 생성은 오히려 오버헤드를 발생시키므로, 정말 무거운 CPU 작업에만 사용해야 한다고 덧붙이면 완벽합니다.



스레드와 아이솔레이트의 차이점은 일반 스레드는 여러 스레드가 같은 메모리 영역을 공유해서 효율적이지만 동시에 접근할 때 데이터 오염이 발생할 수 있어서 Lock(뮤텍스 등)설정이 복잡하고 오버헤드가 발생합니다.
아이솔레이트는 메모리가 완전히 분리되어 있어서 Lock이 필요없어서 UI중단을 최소화 합니다.

***
추가질문.
1. 아이솔레이트를 사용하는 예제를 만들어서 설명해보자. 연쇄적으로 나오는 개념들 연쇄적으로 얼마나 플러터에서 소모되는지?